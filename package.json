{
  "name": "popipo",
  "version": "0.0.1",
  "description": "Simple google meet clone, server part",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.3.0"
  }
}
